
openapi: 3.0.0
info:
  title: Assignment Tracker API
  version: 1.0.0

paths:
  /api/users:
    get:
      summary: Get all users

    post:
      summary: Create user

  /api/assignments:
    get:
      summary: Get assignments

  /api/reminders:
    get:
      summary: Get reminders
